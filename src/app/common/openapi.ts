// import { CollectionApi, Configuration } from '~/lib/openapi';

// export const openapiConfig = new Configuration({
//   basePath: 'http://127.0.0.1:3000/api/model',
// });

// const collectionApi = new CollectionApi(openapiConfig);

// const collectionsResponse = await collectionApi.findManyCollection({
//   q: {
//   }
// })
// collectionsResponse.data.map(c => c.articles)